# Required only when NODE_ENV=production. Use a long, unique random value.
# Keep the real value in .env locally and in your hosting provider's secret settings.
AUTH_SECRET=replace-with-a-long-random-secret
